# Audios de reuniones (pesados, fuente primaria pero no versionable)
*.m4a
*.mp3
*.wav

# Graphify output (derivable con /graphify --update — no versionar)
# Incluye graph.json/html, GRAPH_REPORT.md, cache/, converted/, manifests.
# La config .graphifyignore SÍ se versiona (vive en la raíz).
graphify-out/
**/graphify-out/

# Local Claude Code settings (per-user)
.claude/settings.local.json

# Sistema
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*~
